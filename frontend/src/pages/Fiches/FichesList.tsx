import { useState } from "react";
import { Link } from "react-router-dom";
import Navbar from "../../components/Navbar";
import { fichesApi } from "../../api/fiches.api";
import { authService } from "../../services/auth.service";
export default function FichesList() { const [query, setQuery] = useState(""); const [term, setTerm] = useState(""); const fiches = fichesApi.list().filter((fiche) => fiche.numero.includes(term)); const isCoordinator = authService.currentUser()?.rol === "COORDINADOR"; function submit(event: React.FormEvent) { event.preventDefault(); setTerm(query); } return <><Navbar /><main className="page-shell"><div className="page-heading"><div><p className="eyebrow">Formación</p><h1>Fichas de formación</h1></div>{isCoordinator && <Link className="button" to="/fiches/new">Nueva ficha</Link>}</div><form className="list-search" onSubmit={submit}><input className="search" placeholder="Buscar por número de ficha" value={query} onChange={(event) => setQuery(event.target.value)} /><button className="button" type="submit">Buscar</button></form><div className="table-wrap"><table><thead><tr><th>Ficha</th><th>Programa</th><th>Área</th><th>Trimestre</th><th>Aprendices</th><th>Estado</th>{isCoordinator && <th />}</tr></thead><tbody>{fiches.map((fiche) => <tr key={fiche.id}><td>{fiche.numero}</td><td>{fiche.programa}</td><td>{fiche.area}</td><td>{fiche.trimestre}</td><td>{fiche.aprendices}</td><td>{fiche.estado}</td>{isCoordinator && <td><Link to={`/fiches/${fiche.id}/edit`}>Editar</Link></td>}</tr>)}</tbody></table></div>{fiches.length === 0 && <p>No hay fichas que coincidan con la búsqueda.</p>}</main></>; }
