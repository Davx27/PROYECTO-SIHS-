import { useState } from "react"; import { Link } from "react-router-dom"; import Navbar from "../../components/Navbar"; import { learningResultsApi } from "../../api/learningResults.api"; import { authService } from "../../services/auth.service";
export default function LearningResultsList() { const [query, setQuery] = useState(""); const [term, setTerm] = useState(""); const results = learningResultsApi.list().filter((result) => `${result.nombre} ${result.acronimo}`.toLowerCase().includes(term.toLowerCase())); const isCoordinator = authService.currentUser()?.rol === "COORDINADOR"; function submit(event: React.FormEvent) { event.preventDefault(); setTerm(query); } return <><Navbar /><main className="page-shell"><div className="page-heading"><div><p className="eyebrow">Formación</p><h1>Resultados de aprendizaje</h1></div>{isCoordinator && <Link className="button" to="/learning-results/new">Nuevo resultado</Link>}</div><form className="list-search" onSubmit={submit}><input className="search" placeholder="Buscar por nombre o acrónimo" value={query} onChange={(event) => setQuery(event.target.value)} /><button className="button" type="submit">Buscar</button></form><div className="table-wrap"><table><thead><tr><th>Código</th><th>Nombre</th><th>Acrónimo</th><th>Trimestre</th><th>Horas</th>{isCoordinator && <th />}</tr></thead><tbody>{results.map((result) => <tr key={result.id}><td>{result.codigo}</td><td>{result.nombre}</td><td>{result.acronimo}</td><td>{result.trimestre}</td><td>{result.horas}</td>{isCoordinator && <td><Link to={`/learning-results/${result.id}/edit`}>Editar</Link></td>}</tr>)}</tbody></table></div>{results.length === 0 && <p>No hay resultados que coincidan con la búsqueda.</p>}</main></>; }
