import { useState } from "react"; import Navbar from "../../components/Navbar"; import { Button } from "../../components/ui/Button";
export default function InstructorCode() { const [code, setCode] = useState(""); function generate() { const next = `INS-${Math.random().toString(36).slice(2, 8).toUpperCase()}`; const codes = JSON.parse(localStorage.getItem("sihs_instructor_codes") ?? "[]") as string[]; localStorage.setItem("sihs_instructor_codes", JSON.stringify([...codes, next])); setCode(next); } return <><Navbar /><main className="page-shell"><p className="eyebrow">Administración</p><h1>Código de instructor</h1><p>Genere un código único para habilitar el registro de un instructor.</p><Button type="button" onClick={generate}>Generar código</Button>{code && <p className="generated-code">{code}</p>}</main></>; }
