import { useState } from "react"; import { Link } from "react-router-dom"; import Navbar from "../../components/Navbar"; import { schedulesApi } from "../../api/schedules.api"; import { authService } from "../../services/auth.service";
export default function ScheduleList() { const [query, setQuery] = useState(""); const blocks = schedulesApi.list().filter((item) => item.ficha.includes(query)); const isCoordinator = authService.currentUser()?.rol === "COORDINADOR"; return <><Navbar /><main className="page-shell"><div className="page-heading"><div><p className="eyebrow">Programación</p><h1>Horarios de formación</h1></div>{isCoordinator && <Link className="button" to="/schedules/new">Crear horario</Link>}</div><form className="list-search" onSubmit={(event) => event.preventDefault()}><input className="search" placeholder="Buscar por número de ficha" value={query} onChange={(event) => setQuery(event.target.value)} /><button className="button" type="submit">Buscar</button></form><div className="table-wrap"><table><thead><tr><th>Día</th><th>Jornada</th><th>Bloque</th><th>Ficha</th><th>Instructor</th><th>Ambiente</th>{isCoordinator && <th />}</tr></thead><tbody>{blocks.map((item) => <tr key={item.id}><td>{item.dia}</td><td>{item.jornada}</td><td>{item.bloque}</td><td>{item.ficha}</td><td>{item.instructor}</td><td>{item.ambiente}</td>{isCoordinator && <td><Link to={`/schedules/${item.id}/edit`}>Editar</Link></td>}</tr>)}</tbody></table></div>{blocks.length === 0 && <p>No hay horarios registrados para esta búsqueda.</p>}</main></>; }
