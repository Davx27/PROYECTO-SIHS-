import { Link, useParams } from "react-router-dom"; import Navbar from "../../components/Navbar"; import { usersApi } from "../../api/users.api";
export default function UserDetail() { const { id } = useParams(); const user = usersApi.list().find((item) => item.id === id); if (!user) return <p>Usuario no encontrado.</p>; return <><Navbar /><main className="page-shell"><Link to="/users">Volver a usuarios</Link><p className="eyebrow">Detalle de usuario</p><h1>{user.nombres} {user.apellidos}</h1><div className="detail-list"><p><strong>Documento:</strong> {user.documento}</p><p><strong>Correo:</strong> {user.correo}</p><p><strong>Teléfono:</strong> {user.telefono}</p><p><strong>Rol:</strong> {user.rol}</p></div><Link className="button" to={`/users/${user.id}/edit`}>Editar usuario</Link></main></>; }
